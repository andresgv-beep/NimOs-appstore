{
  "configVersion": 1,
  "configFields": [
    {
      "key": "ADMIN_PASSWORD",
      "label": "Contraseña de administrador",
      "type": "password",
      "required": true,
      "secret": true,
      "hint": "La contraseña para el usuario 'admin' de Grafana. Elígela tú (el usuario es 'admin')."
    }
  ]
}
